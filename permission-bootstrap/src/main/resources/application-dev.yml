server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/permission?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: root123

  data:
    redis:
      host: localhost
      port: 6379

logging:
  level:
    com.permission: DEBUG

swagger:
  enabled: true

springdoc:
  api-docs:
    enabled: true

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

auth:
  jwt:
    secret: dev-jwt-secret-key-for-development-only-at-least-64-characters-long-please-replace-in-production
  users:
    admin: "$2b$12$lSq62um1wy0aBVs27V3HRuxTQtpFt/DImVKkq/Po68eP1NR6gYcwa"
    user1: "$2b$12$BqdLlkaBFh9Lel1piDRp/.yvVZv.bYpOwff4PcgNAPbdiqvDVUSdq"

cors:
  allowed-origins: http://localhost:3000,http://localhost:5173

cookie:
  secure: false
  same-site: Lax

permission:
  super-admins: admin
